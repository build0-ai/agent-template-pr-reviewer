{
  "name": "PR Reviewer",
  "version": "1.0",
  "steps": [
    {
      "id": "clone-and-fetch",
      "type": "command",
      "name": "Clone repo and fetch PR data",
      "command": "rm -rf /workspace/repo && gh repo clone {{ input.repository.full_name }} /workspace/repo -- --depth=100 && cd /workspace/repo && gh pr checkout {{ input.pull_request.number }} --detach && FILES=$(gh pr view {{ input.pull_request.number }} --repo {{ input.repository.full_name }} --json files --jq '.files | map(\"- \\(.path) (+\\(.additions)/-\\(.deletions))\") | join(\"\\n\")') && DIFF=$(gh pr diff {{ input.pull_request.number }} --repo {{ input.repository.full_name }}) && if [ ${#DIFF} -gt 100000 ]; then DIFF=\"${DIFF:0:100000}\n... (truncated)\"; fi && jq -n --arg files \"$FILES\" --arg diff \"$DIFF\" '{files: $files, diff: $diff}'",
      "env": {
        "GH_TOKEN": "{{ credentials['github-app-oauth'].access_token }}"
      },
      "retries": 2,
      "retryDelay": 3000
    },
    {
      "id": "generate-review",
      "type": "command",
      "name": "Review PR",
      "command": "cd /workspace/repo && npx tsx /workspace/agent/src/review.ts",
      "params": {
        "prompt": "Review PR #{{ input.pull_request.number }}: {{ input.pull_request.title }}\n\nDescription: {{ input.pull_request.body }}\n\nChanged files:\n{{ JSON.parse(steps['clone-and-fetch'].output).files }}\n\nDiff:\n```diff\n{{ JSON.parse(steps['clone-and-fetch'].output).diff }}\n```\n\n## Instructions\nReview this PR. Use Read/Grep tools to check context if needed.\n\n## Rules\n1. **Summary**: 2-3 sentences max. State what the PR does what it says it does and if it's ready to merge.\n2. **Comments**: Only flag issues that actually matter:\n   - Bugs, security issues, or broken logic → critical/warning\n   - Suggestions that meaningfully improve the code → suggestion\n   - Do NOT add nitpicks, style preferences, or observations that don't require action\n3. **Line numbers**: ONLY comment on lines that appear in the diff with + or - prefix. Comments on unchanged lines will fail to post.\n4. **Decision**: APPROVE if good, REQUEST_CHANGES if blocking issues exist, COMMENT otherwise.\n\nBe direct. Less is more."
      },
      "env": {
        "IS_SANDBOX": "1",
        "GITHUB_TOKEN": "{{ credentials['github-app-oauth'].access_token }}",
        "ANTHROPIC_API_KEY": "{{ credentials['anthropic-api'].ANTHROPIC_API_KEY }}",
        "ANTHROPIC_BASE_URL": "{{ credentials['anthropic-api'].ANTHROPIC_BASE_URL }}",
        "PROMPT": "{{ params.prompt }}",
        "PR_NUMBER": "{{ input.pull_request.number }}",
        "REPO": "{{ input.repository.full_name }}",
        "COMMIT_SHA": "{{ input.pull_request.head.sha }}"
      }
    },
    {
      "id": "save-checkpoint",
      "type": "command",
      "name": "Save checkpoint",
      "command": "cd /workspace/repo && COMMIT_SHA=$(git rev-parse HEAD) && echo '{\"timestamp\": \"'$(date -u +%Y-%m-%dT%H:%M:%SZ)'\", \"commit_sha\": \"'$COMMIT_SHA'\"}' | jq -c ."
    },
    {
      "id": "poll-wait",
      "type": "sleep",
      "name": "Wait for updates",
      "duration": 120000
    },
    {
      "id": "check-pr-and-activity",
      "type": "command",
      "name": "Check PR status and activity",
      "command": "PR_DATA=$(gh pr view {{ input.pull_request.number }} --repo {{ input.repository.full_name }} --json state,headRefOid) && STATE=$(echo \"$PR_DATA\" | jq -r '.state') && if [ \"$STATE\" != 'OPEN' ]; then echo \"$STATE\" && exit 1; fi && CURRENT_SHA=$(echo \"$PR_DATA\" | jq -r '.headRefOid') && LAST_SHA=$(printf '%s' \"$CHECKPOINT_JSON\" | jq -r '.commit_sha') && HAS_NEW_COMMITS=false && if [ \"$LAST_SHA\" != \"$CURRENT_SHA\" ]; then cd /workspace/repo && gh pr checkout {{ input.pull_request.number }} --detach --force >&2 && HAS_NEW_COMMITS=true; fi && SINCE=$(printf '%s' \"$CHECKPOINT_JSON\" | jq -r '.timestamp') && NEW_COMMENTS=$(gh api repos/{{ input.repository.full_name }}/pulls/{{ input.pull_request.number }}/comments --jq \"[.[] | select(.created_at > \\\"$SINCE\\\" and (.user.login | contains(\\\"[bot]\\\") | not) and (.body | contains(\\\"🤖\\\") | not))]\") && jq -n --argjson has_new_commits $HAS_NEW_COMMITS --arg current_sha \"$CURRENT_SHA\" --argjson new_comments \"$NEW_COMMENTS\" '{has_new_commits: $has_new_commits, current_sha: $current_sha, new_comments: $new_comments}'",
      "env": {
        "GH_TOKEN": "{{ credentials['github-app-oauth'].access_token }}",
        "CHECKPOINT_JSON": "{{ steps['save-checkpoint'].output }}"
      },
      "onError": "goto",
      "onErrorGoto": "is-review-completed"
    },
    {
      "id": "respond-to-activity",
      "type": "command",
      "name": "Respond to new activity",
      "if": "{{ JSON.parse(steps['check-pr-and-activity'].output).has_new_commits || JSON.parse(steps['check-pr-and-activity'].output).new_comments.length > 0 }}",
      "command": "cd /workspace/repo && npx tsx /workspace/agent/src/respond.ts",
      "params": {
        "prompt": "New activity on the PR:\n\n### New Comments\n{{ JSON.stringify(JSON.parse(steps['check-pr-and-activity'].output).new_comments) }}\n\n### New Commits\n{{ JSON.parse(steps['check-pr-and-activity'].output).has_new_commits }}\n\nRespond to comments and/or acknowledge new commits. If significant changes need a full re-review, set should_re_review to true."
      },
      "env": {
        "IS_SANDBOX": "1",
        "GITHUB_TOKEN": "{{ credentials['github-app-oauth'].access_token }}",
        "ANTHROPIC_API_KEY": "{{ credentials['anthropic-api'].ANTHROPIC_API_KEY }}",
        "ANTHROPIC_BASE_URL": "{{ credentials['anthropic-api'].ANTHROPIC_BASE_URL }}",
        "PR_NUMBER": "{{ input.pull_request.number }}",
        "REPO": "{{ input.repository.full_name }}",
        "HAS_NEW_COMMITS": "{{ JSON.parse(steps['check-pr-and-activity'].output).has_new_commits }}",
        "NEW_COMMENTS": "{{ JSON.stringify(JSON.parse(steps['check-pr-and-activity'].output).new_comments) }}",
        "PROMPT_TEMPLATE": "{{ params.prompt }}"
      }
    },
    {
      "id": "do-re-review",
      "type": "command",
      "name": "Re-review PR",
      "command": "cd /workspace/repo && npx tsx /workspace/agent/src/review.ts",
      "if": "{{ JSON.parse(steps['respond-to-activity']?.output || '{}').re_review }}",
      "params": {
        "prompt": "New commits have been pushed. Re-review the PR with the same criteria as before."
      },
      "env": {
        "IS_SANDBOX": "1",
        "GITHUB_TOKEN": "{{ credentials['github-app-oauth'].access_token }}",
        "ANTHROPIC_API_KEY": "{{ credentials['anthropic-api'].ANTHROPIC_API_KEY }}",
        "ANTHROPIC_BASE_URL": "{{ credentials['anthropic-api'].ANTHROPIC_BASE_URL }}",
        "PROMPT": "{{ params.prompt }}",
        "PR_NUMBER": "{{ input.pull_request.number }}",
        "REPO": "{{ input.repository.full_name }}",
        "COMMIT_SHA": "{{ JSON.parse(steps['check-pr-and-activity'].output).current_sha }}"
      }
    },
    {
      "id": "is-review-completed",
      "type": "command",
      "name": "Check if review is completed",
      "command": "if echo \"$LAST_OUTPUT\" | grep -q '^{'; then exit 1; fi",
      "env": {
        "LAST_OUTPUT": "{{ steps['check-pr-and-activity'].output }}"
      },
      "onError": "goto",
      "onErrorGoto": "save-checkpoint"
    },
    {
      "id": "wrap-up",
      "type": "analytics",
      "name": "Wrap up",
      "events": [
        {
          "name": "pr_reviewed",
          "type": "counter",
          "category": "outcomes"
        },
        {
          "name": "pr_approved",
          "value": "{{ JSON.parse(steps['generate-review'].output).decision === 'APPROVE' ? 1 : 0 }}",
          "type": "counter",
          "category": "outcomes"
        },
        {
          "name": "pr_changes_requested",
          "value": "{{ JSON.parse(steps['generate-review'].output).decision === 'REQUEST_CHANGES' ? 1 : 0 }}",
          "type": "counter",
          "category": "outcomes"
        },
        {
          "name": "review_comments",
          "value": "{{ JSON.parse(steps['generate-review'].output).comments_count }}",
          "type": "gauge",
          "category": "engagement"
        },
        {
          "name": "replies_posted",
          "value": "{{ JSON.parse(steps['respond-to-activity']?.output || '{\"replies_count\":0}').replies_count }}",
          "type": "gauge",
          "category": "engagement"
        }
      ]
    }
  ]
}
